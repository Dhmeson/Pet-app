import { Text, View } from "react-native";
import { Box } from "../../../components/Box";

export function PetHealth() {
	return (
		<View>
			<Box.Root>{/* <Box.Title text='Vaciinação' > </Box.Title> */}</Box.Root>
		</View>
	);
}
